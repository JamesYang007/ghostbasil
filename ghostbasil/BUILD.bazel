cc_library(
    name = "ghostbasil",
    hdrs = glob(["include/**"]),
    includes = ["include"],
    visibility = ["//visibility:public"],
    defines = [
        "EIGEN_MATRIXBASE_PLUGIN=\\\"ghostbasil/eigen_matrixbase_plugin.hpp\\\"",
    ],
    deps = [
        "@eigen",
    ],
)
